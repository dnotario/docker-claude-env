# Express.js API Starter

Simple Express.js REST API starter template.

## Setup

```bash
npm install
```

## Run

```bash
# Development (with auto-reload)
npm run dev

# Production
npm start
```

## Test

```bash
# In another terminal
curl http://localhost:3000/
curl http://localhost:3000/health
curl http://localhost:3000/hello/world
```

## Features

- Express.js 4.x
- JSON middleware
- Basic routing
- Health check endpoint
- Development with nodemon

## Next Steps

- Add database connection (MongoDB, PostgreSQL, etc.)
- Add authentication (JWT, Passport, etc.)
- Add validation (Joi, express-validator, etc.)
- Add tests (Jest, Mocha, etc.)
- Add environment configuration
